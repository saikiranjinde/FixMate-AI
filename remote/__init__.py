"""Additive Remote Diagnostic Mode for FixMate-AI.

This package is intentionally standalone and does not modify the existing
FixMate-AI core/UI files. The target PC runs the agent and the controller PC
connects to it over the local network.
"""
